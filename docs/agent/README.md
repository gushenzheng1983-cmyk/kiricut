# Agent 工作区

Cursor / AI 在本项目里**新建的文件**默认放这里（或 `test-samples/` 测图）。

## 子目录

| 目录 | 放什么 |
|------|--------|
| `docs/agent/notes/` | 会话备忘、整理说明、分析结论 |
| `docs/agent/exports/` | 导出的清单、对比表等（非代码） |
| `test-samples/` | 测试用图片 |

## 规则

- 项目根目录：**`C:\Users\feng3\kiricut`**
- 没有合适文件夹就**自己建**，不要写到 `.cursor\projects\` 或其它盘
- 代码改 `src/`；文档改 `docs/`；测图放 `test-samples/`
